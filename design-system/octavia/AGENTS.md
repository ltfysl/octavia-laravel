# AGENTS.md — design-system/octavia

## What lives here
The Octavia design system master document and page specs.

## Rules
- `MASTER.md` owns the overall visual language; sub-specs live under `pages/`.
- Keep specifications concrete (tokens, values, behavior).
- Align with `designs/` and the implementation in `resources/js/components/ui`.
