# AGENTS.md — public

## What lives here
Static web assets and the front controller: .htaccess, favicon.ico, favicon.svg, index.php, robots.txt.

## Rules
- Only static, publicly served files here; compiled assets go to `public/build/` (ignored).
- Do not commit `public/build/` output.
- Keep `robots.txt`, `favicon` and `.htaccess` current.
