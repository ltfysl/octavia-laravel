# AGENTS.md — resources/js/utils

## What lives here
Small pure frontend utility functions: diff.ts.

## Rules
- Keep functions pure, typed and domain-agnostic where possible.
- Reuse utilities across pages; do not copy snippets.
- `diff.ts` handles prompt diff rendering.
