# Round 20 — Team Invitation Emails

## Current Product State
Nineteen rounds in. Credits/billing foundation landed. Team invites existed but were silent — the invitee only discovered membership by noticing shared items. This round closed the loop.

## Selected Goals
1. Notify invitees when added to a team (mail + in-app bell), queueable and localized.

## Implementation Summary
- **`TeamInvitationNotification`**: queueable, mail + database channels. Mail uses the invitee's stored locale (`teams.invitation.*` EN/DE), names the inviter, team and localized role; database payload carries team_id/name, inviter and role for the bell dropdown.
- **`TeamController::invite`**: sends the notification after membership creation.

## Tests / Validation
- TeamTest: +1 — invitee receives the notification on both channels with the correct role payload.
- Full suite: **114 tests / 392 assertions green**. Pint clean, vue-tsc clean, build green.

## Remaining Problems
- Stripe integration remains human-blocked (account/keys) — checklist in docs/operations/billing.md.

## Human Actions Required
Unchanged: OpenAI-compatible key, production domain/TLS, Stripe account when paid plans launch.
