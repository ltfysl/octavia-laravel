# AGENTS.md — scripts

## What lives here
One-off helper scripts: create-e2e-db.php, smoke.mjs.

## Rules
- Document purpose and usage in the file header.
- `create-e2e-db.php` prepares the E2E database; `smoke.mjs` runs smoke checks.
- Never commit sensitive data or production credentials.
