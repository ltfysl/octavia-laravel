# AGENTS.md — resources

## What lives here
Frontend source assets: JS, CSS, Blade views and locales.

## Rules
- Prefer reusable components over page-specific markup duplication.
- Theme tokens in `resources/css/app.css` are the single source of style truth.
- Keep i18n keys in `resources/js/locales/` and backend in `lang/`.
